apiVersion: v1
kind: Service
metadata:
  name: angular-app-service
  labels:
    app: angular-app
spec:
  selector:
    app: angular-app
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 80
